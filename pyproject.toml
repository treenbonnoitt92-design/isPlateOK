[project]
name = "jevlaya"
version = "0.1.0"
description = "Jev & Laya AI License Plate Matching System"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]

[project.optional-dependencies]
cuda = [
    "torch",
    "laya",
]
